# Add project specific ProGuard rules here.
# React Native ships its own consumer rules via the gradle plugin; release builds
# keep minification disabled (see app/build.gradle), so this file is intentionally
# minimal. Add app-specific keep rules here if minification is enabled later.
